version: "3.5"

services:
  pg:
    image: postgres:12
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
  app:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./app:/var/app
    command: uvicorn main:app
    depends_on:
      - pg
